* OPTIONAL: PSX image files

The audio on the PC port is poor. By providing PSX image files here, the extractor will automatically extract the background audio from the PSX files instead.

Everything else is from the PC files (which is essentially identical, but easier to parse)